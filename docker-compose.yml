version: '3.8'

services:
  tor:
    image: dperson/torproxy
    ports:
      - "9050:9050"
    restart: always

  professor-osint:
    build: .
    volumes:
      - .:/app
    depends_on:
      - tor
    # Example usage: docker-compose run professor-osint -q "example.com" --tor
